import { T_STRING, T_STRING_ARRAY } from '../core/types';
export declare const hasAny: (data: T_STRING_ARRAY, value: any, all?: T_STRING) => boolean;
export declare const hasAll: (data: T_STRING_ARRAY, values: any[], all?: T_STRING) => boolean;
